/**
 * Development demo account.
 *
 * A real PostgreSQL row created by `backend/scripts/seed_demo_user.py` using
 * the same hashing path as normal registration. Development-only credentials;
 * run the seed script if login fails.
 */
export const demoCredentials = { email: 'demo@pharmapanda.app', password: 'pharmapanda-dev' }
