# Development and test dependencies.
#
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Not installed in production: a test runner on a live server is unused
# attack surface.

-r requirements.txt

pytest==8.3.5
httpx==0.28.1
